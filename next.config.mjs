/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ["avatar.iran.liara.run", "lh3.googleusercontent.com", "api.qrserver.com"],
    },
};

export default nextConfig;

